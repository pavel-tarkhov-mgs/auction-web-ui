{
  "name": "auction-web-ui",
  "version": "1.0.0",
  "description": "Technical web UI for testing the Auction gRPC service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.10.0",
    "@grpc/proto-loader": "^0.7.10",
    "dotenv": "^16.4.0",
    "express": "^4.18.0"
  }
}
